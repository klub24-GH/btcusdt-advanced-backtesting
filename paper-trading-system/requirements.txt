# No external dependencies - uses Python standard library only
# This paper trading system is designed to run with zero external packages
# for maximum compatibility and easy deployment

# Optional: If you want to add web framework later
# fastapi==0.104.1
# uvicorn==0.24.0